apiVersion: v1
kind: Service
metadata:
  name: homepage-web-service
  namespace: homepage
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  selector:
    app: homepage-web